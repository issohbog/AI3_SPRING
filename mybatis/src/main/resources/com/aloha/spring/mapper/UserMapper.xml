<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace="매퍼 인터페이스 경로" -->
<mapper namespace="com.aloha.spring.mapper.UserMapper">  <!-- UserMapper 인터페이스와 연결하겠다는 의미 -->
	

	
	<!-- 회원 등록(가입)  -->
	<insert id="insert" parameterType="com.aloha.spring.dto.User" >
		INSERT INTO user (id, username, password, name, email)
		VALUES (#{id}, #{username}, #{password}, #{name}, #{email})
	</insert>	
	 
	<!-- 로그인용 회원 조회  -->
	<select id="select" parameterType="map" resultType="User">
		SELECT * 
		FROM user 
		WHERE id = #{id} AND password = #{password}
	</select>
	
	
	
	

</mapper>